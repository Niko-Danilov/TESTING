class DASHBORD {
	private root = 'http://localhost:3000'

	HOME = `${this.root}`
	ADDPRODUCT = `${this.root}/AddProduct`
	APIDATA = `${this.root}/api/data`
	APIAUTH = `${this.root}/api/auth`
}

export const DASHBORD_PAGES = new DASHBORD()
